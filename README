this starter kit has a bug
in short as soon as react-navigation
(latest version) is introduced
the app launches and then quits

If I remove the nav libs the host
can load the empty (only containing text)
remotes fine. But when I add react-navigation
those screen displays without navig displayed
but with code within the stacks component actually running (HS2!!)

Even when downgrading to older models I get
a different issue of a red screen with
a tags suggesting that theres a version mismatch.

does this latest version of v2 work with the latest
versions of react-navigation?

whether I put react-navigation in host
or where I wanted to put it in deepl-link-dyn-navigation
remote it renders nothing even though I can see a log from the
Stack.Screen's component.

